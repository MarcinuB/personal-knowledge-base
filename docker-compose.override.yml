services:
  frontend:
    build:
      context: ./frontend
      target: dev
    command: npm run dev -- --host
    ports:
      - "3000:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://backend:8000
