[
  {
    "user_input": "How is PostgreSQL utilized in the architecture of the Personal Knowledge Base?",
    "reference_contexts": [
      "Personal Knowledge Base \u2014 Architecture Overview\n\nThe Personal Knowledge Base is a self-hosted application that lets users ingest documents and query them using a Retrieval-Augmented Generation (RAG) pipeline. It is built with FastAPI for the HTTP layer, PostgreSQL for relational metadata, ChromaDB for vector storage, and LangGraph for orchestrating the chat pipeline.\n\n Document Ingestion\n\nDocuments can be ingested in two ways: manual file upload and automated connector sync.\n\nFile uploads are accepted via a POST request to /api/collections/{collection_id}/documents. Supported formats are PDF, DOCX, and TXT. The raw file bytes are parsed into plain text using LangChain document loaders: PyPDFLoader for PDFs, Docx2txtLoader for Word documents, and TextLoader for plain text files.\n\nConnectors are registered components that implement a BaseConnector interface. Each connector exposes an async generator called sync() that yields LangChain Document objects. A DummyConnector is provided for smoke testing. Connectors are registered at application startup and can be triggered via POST /api/connectors/{name}/sync.\n\n Chunking Strategy\n\nParsed text is split using a two-level hierarchical chunking approach implemented with LangChain's RecursiveCharacterTextSplitter. Parent chunks are 3200 characters with no overlap, while child chunks are 800 characters with 80 characters of overlap. Child chunks are what gets embedded and stored in ChromaDB. Each chunk carries metadata including a parent_id, the full parent_text, the doc_id, and the collection_id. During retrieval, child chunks are used for similarity search but the larger parent text is returned to the language model, providing richer context.\n\n Upsert Behaviour\n\nWhen a document is re-uploaded or a connector re-syncs a previously seen source, the system performs a content-aware upsert. Each document row stores a source_uri (the filename or connector source path) and a SHA-256 content_hash of the parsed text. On ingest, the system looks up any existing document with the same (collection_id, source_uri). If the hash matches, ingestion is skipped entirely. If the hash differs, new chunks are written first, then the old document row and its ChromaDB chunks are deleted \u2014 ensuring the old version is never lost due to a failed re-ingest.\n\n The Chat Pipeline\n\nWhen a user sends a message, the application runs a five-node LangGraph state machine.\n\nThe collection_router node checks whether the target ChromaDB collection exists and contains documents. If it is empty or missing, it returns an early exit message without invoking the language model.\n\nThe query_rewriter node reformulates the user's question to be standalone. On the first message of a conversation it passes the question through unchanged. On follow-up messages it uses the language model to rewrite the question using the last six messages of history, optimising it for document retrieval.\n\nThe retriever node embeds the rewritten query and performs a nearest-neighbour search in ChromaDB, fetching the top ten results. Results are deduplicated by parent_id and the unique parent texts are passed forward.\n\nThe reranker node applies a cross-encoder model (cross-encoder/ms-marco-MiniLM-L-6-v2 from sentence-transformers) to score each query-chunk pair. The top five chunks by score are selected.\n\nThe generator node constructs a system prompt that instructs the language model to answer only using the provided context chunks. It appends the conversation history and the current user message, then streams the response token by token back to the client via Server-Sent Events.\n\n"
    ],
    "reference": "PostgreSQL is used for relational metadata in the architecture of the Personal Knowledge Base, which is a self-hosted application that allows users to ingest documents and query them using a Retrieval-Augmented Generation (RAG) pipeline.",
    "persona_name": "Knowledge Management Engineer",
    "query_style": "WEB_SEARCH_LIKE",
    "query_length": "MEDIUM",
    "synthesizer_name": "single_hop_specific_query_synthesizer"
  },
  {
    "user_input": "What role does ChromaDB play in the architecture of the Personal Knowledge Base?",
    "reference_contexts": [
      "Personal Knowledge Base \u2014 Architecture Overview\n\nThe Personal Knowledge Base is a self-hosted application that lets users ingest documents and query them using a Retrieval-Augmented Generation (RAG) pipeline. It is built with FastAPI for the HTTP layer, PostgreSQL for relational metadata, ChromaDB for vector storage, and LangGraph for orchestrating the chat pipeline.\n\n Document Ingestion\n\nDocuments can be ingested in two ways: manual file upload and automated connector sync.\n\nFile uploads are accepted via a POST request to /api/collections/{collection_id}/documents. Supported formats are PDF, DOCX, and TXT. The raw file bytes are parsed into plain text using LangChain document loaders: PyPDFLoader for PDFs, Docx2txtLoader for Word documents, and TextLoader for plain text files.\n\nConnectors are registered components that implement a BaseConnector interface. Each connector exposes an async generator called sync() that yields LangChain Document objects. A DummyConnector is provided for smoke testing. Connectors are registered at application startup and can be triggered via POST /api/connectors/{name}/sync.\n\n Chunking Strategy\n\nParsed text is split using a two-level hierarchical chunking approach implemented with LangChain's RecursiveCharacterTextSplitter. Parent chunks are 3200 characters with no overlap, while child chunks are 800 characters with 80 characters of overlap. Child chunks are what gets embedded and stored in ChromaDB. Each chunk carries metadata including a parent_id, the full parent_text, the doc_id, and the collection_id. During retrieval, child chunks are used for similarity search but the larger parent text is returned to the language model, providing richer context.\n\n Upsert Behaviour\n\nWhen a document is re-uploaded or a connector re-syncs a previously seen source, the system performs a content-aware upsert. Each document row stores a source_uri (the filename or connector source path) and a SHA-256 content_hash of the parsed text. On ingest, the system looks up any existing document with the same (collection_id, source_uri). If the hash matches, ingestion is skipped entirely. If the hash differs, new chunks are written first, then the old document row and its ChromaDB chunks are deleted \u2014 ensuring the old version is never lost due to a failed re-ingest.\n\n The Chat Pipeline\n\nWhen a user sends a message, the application runs a five-node LangGraph state machine.\n\nThe collection_router node checks whether the target ChromaDB collection exists and contains documents. If it is empty or missing, it returns an early exit message without invoking the language model.\n\nThe query_rewriter node reformulates the user's question to be standalone. On the first message of a conversation it passes the question through unchanged. On follow-up messages it uses the language model to rewrite the question using the last six messages of history, optimising it for document retrieval.\n\nThe retriever node embeds the rewritten query and performs a nearest-neighbour search in ChromaDB, fetching the top ten results. Results are deduplicated by parent_id and the unique parent texts are passed forward.\n\nThe reranker node applies a cross-encoder model (cross-encoder/ms-marco-MiniLM-L-6-v2 from sentence-transformers) to score each query-chunk pair. The top five chunks by score are selected.\n\nThe generator node constructs a system prompt that instructs the language model to answer only using the provided context chunks. It appends the conversation history and the current user message, then streams the response token by token back to the client via Server-Sent Events.\n\n"
    ],
    "reference": "ChromaDB is used for vector storage in the Personal Knowledge Base architecture, where child chunks of parsed text are embedded and stored for retrieval during the query process.",
    "persona_name": "Knowledge Management Engineer",
    "query_style": "PERFECT_GRAMMAR",
    "query_length": "MEDIUM",
    "synthesizer_name": "single_hop_specific_query_synthesizer"
  },
  {
    "user_input": "What is the role of Docx2txtLoader in the document ingestion process of the Personal Knowledge Base?",
    "reference_contexts": [
      "Personal Knowledge Base \u2014 Architecture Overview\n\nThe Personal Knowledge Base is a self-hosted application that lets users ingest documents and query them using a Retrieval-Augmented Generation (RAG) pipeline. It is built with FastAPI for the HTTP layer, PostgreSQL for relational metadata, ChromaDB for vector storage, and LangGraph for orchestrating the chat pipeline.\n\n Document Ingestion\n\nDocuments can be ingested in two ways: manual file upload and automated connector sync.\n\nFile uploads are accepted via a POST request to /api/collections/{collection_id}/documents. Supported formats are PDF, DOCX, and TXT. The raw file bytes are parsed into plain text using LangChain document loaders: PyPDFLoader for PDFs, Docx2txtLoader for Word documents, and TextLoader for plain text files.\n\nConnectors are registered components that implement a BaseConnector interface. Each connector exposes an async generator called sync() that yields LangChain Document objects. A DummyConnector is provided for smoke testing. Connectors are registered at application startup and can be triggered via POST /api/connectors/{name}/sync.\n\n Chunking Strategy\n\nParsed text is split using a two-level hierarchical chunking approach implemented with LangChain's RecursiveCharacterTextSplitter. Parent chunks are 3200 characters with no overlap, while child chunks are 800 characters with 80 characters of overlap. Child chunks are what gets embedded and stored in ChromaDB. Each chunk carries metadata including a parent_id, the full parent_text, the doc_id, and the collection_id. During retrieval, child chunks are used for similarity search but the larger parent text is returned to the language model, providing richer context.\n\n Upsert Behaviour\n\nWhen a document is re-uploaded or a connector re-syncs a previously seen source, the system performs a content-aware upsert. Each document row stores a source_uri (the filename or connector source path) and a SHA-256 content_hash of the parsed text. On ingest, the system looks up any existing document with the same (collection_id, source_uri). If the hash matches, ingestion is skipped entirely. If the hash differs, new chunks are written first, then the old document row and its ChromaDB chunks are deleted \u2014 ensuring the old version is never lost due to a failed re-ingest.\n\n The Chat Pipeline\n\nWhen a user sends a message, the application runs a five-node LangGraph state machine.\n\nThe collection_router node checks whether the target ChromaDB collection exists and contains documents. If it is empty or missing, it returns an early exit message without invoking the language model.\n\nThe query_rewriter node reformulates the user's question to be standalone. On the first message of a conversation it passes the question through unchanged. On follow-up messages it uses the language model to rewrite the question using the last six messages of history, optimising it for document retrieval.\n\nThe retriever node embeds the rewritten query and performs a nearest-neighbour search in ChromaDB, fetching the top ten results. Results are deduplicated by parent_id and the unique parent texts are passed forward.\n\nThe reranker node applies a cross-encoder model (cross-encoder/ms-marco-MiniLM-L-6-v2 from sentence-transformers) to score each query-chunk pair. The top five chunks by score are selected.\n\nThe generator node constructs a system prompt that instructs the language model to answer only using the provided context chunks. It appends the conversation history and the current user message, then streams the response token by token back to the client via Server-Sent Events.\n\n"
    ],
    "reference": "Docx2txtLoader is used to parse DOCX files into plain text during the document ingestion process of the Personal Knowledge Base. It is one of the LangChain document loaders that convert raw file bytes into a format suitable for storage and retrieval.",
    "persona_name": "Knowledge Management Engineer",
    "query_style": "MISSPELLED",
    "query_length": "LONG",
    "synthesizer_name": "single_hop_specific_query_synthesizer"
  },
  {
    "user_input": "What is this UUID thing and how it work with PostgreSQL and ChromaDB in storage?",
    "reference_contexts": [
      "Storage\n\nPostgreSQL stores collections, documents, conversations, messages, and connector sync state. ChromaDB stores the vector embeddings and chunk metadata. The two stores are linked by the collection UUID, which is used as the ChromaDB collection name. Deleting a collection removes both the PostgreSQL row and the corresponding ChromaDB collection.\n"
    ],
    "reference": "The UUID is used as the ChromaDB collection name, linking the two stores, PostgreSQL and ChromaDB. When a collection is deleted, it removes both the PostgreSQL row and the corresponding ChromaDB collection.",
    "persona_name": "Knowledge Management Engineer",
    "query_style": "POOR_GRAMMAR",
    "query_length": "LONG",
    "synthesizer_name": "single_hop_specific_query_synthesizer"
  },
  {
    "user_input": "What does ChromaDB store?",
    "reference_contexts": [
      "Storage\n\nPostgreSQL stores collections, documents, conversations, messages, and connector sync state. ChromaDB stores the vector embeddings and chunk metadata. The two stores are linked by the collection UUID, which is used as the ChromaDB collection name. Deleting a collection removes both the PostgreSQL row and the corresponding ChromaDB collection.\n"
    ],
    "reference": "ChromaDB stores the vector embeddings and chunk metadata.",
    "persona_name": "Knowledge Management Engineer",
    "query_style": "WEB_SEARCH_LIKE",
    "query_length": "SHORT",
    "synthesizer_name": "single_hop_specific_query_synthesizer"
  },
  {
    "user_input": "How does ChromaDB interact with PostgreSQL in terms of data storage and management?",
    "reference_contexts": [
      "Storage\n\nPostgreSQL stores collections, documents, conversations, messages, and connector sync state. ChromaDB stores the vector embeddings and chunk metadata. The two stores are linked by the collection UUID, which is used as the ChromaDB collection name. Deleting a collection removes both the PostgreSQL row and the corresponding ChromaDB collection.\n"
    ],
    "reference": "ChromaDB stores the vector embeddings and chunk metadata, while PostgreSQL stores collections, documents, conversations, messages, and connector sync state. The two stores are linked by the collection UUID, which serves as the ChromaDB collection name. When a collection is deleted, it removes both the PostgreSQL row and the corresponding ChromaDB collection.",
    "persona_name": "Knowledge Management Engineer",
    "query_style": "WEB_SEARCH_LIKE",
    "query_length": "LONG",
    "synthesizer_name": "single_hop_specific_query_synthesizer"
  },
  {
    "user_input": "How does ChromaDB interact with the Personal Knowledge Base's document ingestion and storage processes?",
    "reference_contexts": [
      "<1-hop>\n\nPersonal Knowledge Base \u2014 Architecture Overview\n\nThe Personal Knowledge Base is a self-hosted application that lets users ingest documents and query them using a Retrieval-Augmented Generation (RAG) pipeline. It is built with FastAPI for the HTTP layer, PostgreSQL for relational metadata, ChromaDB for vector storage, and LangGraph for orchestrating the chat pipeline.\n\n Document Ingestion\n\nDocuments can be ingested in two ways: manual file upload and automated connector sync.\n\nFile uploads are accepted via a POST request to /api/collections/{collection_id}/documents. Supported formats are PDF, DOCX, and TXT. The raw file bytes are parsed into plain text using LangChain document loaders: PyPDFLoader for PDFs, Docx2txtLoader for Word documents, and TextLoader for plain text files.\n\nConnectors are registered components that implement a BaseConnector interface. Each connector exposes an async generator called sync() that yields LangChain Document objects. A DummyConnector is provided for smoke testing. Connectors are registered at application startup and can be triggered via POST /api/connectors/{name}/sync.\n\n Chunking Strategy\n\nParsed text is split using a two-level hierarchical chunking approach implemented with LangChain's RecursiveCharacterTextSplitter. Parent chunks are 3200 characters with no overlap, while child chunks are 800 characters with 80 characters of overlap. Child chunks are what gets embedded and stored in ChromaDB. Each chunk carries metadata including a parent_id, the full parent_text, the doc_id, and the collection_id. During retrieval, child chunks are used for similarity search but the larger parent text is returned to the language model, providing richer context.\n\n Upsert Behaviour\n\nWhen a document is re-uploaded or a connector re-syncs a previously seen source, the system performs a content-aware upsert. Each document row stores a source_uri (the filename or connector source path) and a SHA-256 content_hash of the parsed text. On ingest, the system looks up any existing document with the same (collection_id, source_uri). If the hash matches, ingestion is skipped entirely. If the hash differs, new chunks are written first, then the old document row and its ChromaDB chunks are deleted \u2014 ensuring the old version is never lost due to a failed re-ingest.\n\n The Chat Pipeline\n\nWhen a user sends a message, the application runs a five-node LangGraph state machine.\n\nThe collection_router node checks whether the target ChromaDB collection exists and contains documents. If it is empty or missing, it returns an early exit message without invoking the language model.\n\nThe query_rewriter node reformulates the user's question to be standalone. On the first message of a conversation it passes the question through unchanged. On follow-up messages it uses the language model to rewrite the question using the last six messages of history, optimising it for document retrieval.\n\nThe retriever node embeds the rewritten query and performs a nearest-neighbour search in ChromaDB, fetching the top ten results. Results are deduplicated by parent_id and the unique parent texts are passed forward.\n\nThe reranker node applies a cross-encoder model (cross-encoder/ms-marco-MiniLM-L-6-v2 from sentence-transformers) to score each query-chunk pair. The top five chunks by score are selected.\n\nThe generator node constructs a system prompt that instructs the language model to answer only using the provided context chunks. It appends the conversation history and the current user message, then streams the response token by token back to the client via Server-Sent Events.\n\n",
      "<2-hop>\n\nStorage\n\nPostgreSQL stores collections, documents, conversations, messages, and connector sync state. ChromaDB stores the vector embeddings and chunk metadata. The two stores are linked by the collection UUID, which is used as the ChromaDB collection name. Deleting a collection removes both the PostgreSQL row and the corresponding ChromaDB collection.\n"
    ],
    "reference": "ChromaDB plays a crucial role in the Personal Knowledge Base by storing the vector embeddings and chunk metadata of the ingested documents. When documents are uploaded, they are parsed into child chunks, which are then embedded and stored in ChromaDB. The system ensures that each chunk carries metadata, including a parent_id and doc_id, facilitating similarity searches during retrieval. Additionally, ChromaDB collections are linked to PostgreSQL by the collection UUID, ensuring that deleting a collection removes both the PostgreSQL row and the corresponding ChromaDB collection.",
    "synthesizer_name": "multi_hop_specific_query_synthesizer"
  },
  {
    "user_input": "How does ChromaDB facilitate the storage and retrieval of document chunks in the Personal Knowledge Base, and what role does PostgreSQL play in this architecture?",
    "reference_contexts": [
      "<1-hop>\n\nPersonal Knowledge Base \u2014 Architecture Overview\n\nThe Personal Knowledge Base is a self-hosted application that lets users ingest documents and query them using a Retrieval-Augmented Generation (RAG) pipeline. It is built with FastAPI for the HTTP layer, PostgreSQL for relational metadata, ChromaDB for vector storage, and LangGraph for orchestrating the chat pipeline.\n\n Document Ingestion\n\nDocuments can be ingested in two ways: manual file upload and automated connector sync.\n\nFile uploads are accepted via a POST request to /api/collections/{collection_id}/documents. Supported formats are PDF, DOCX, and TXT. The raw file bytes are parsed into plain text using LangChain document loaders: PyPDFLoader for PDFs, Docx2txtLoader for Word documents, and TextLoader for plain text files.\n\nConnectors are registered components that implement a BaseConnector interface. Each connector exposes an async generator called sync() that yields LangChain Document objects. A DummyConnector is provided for smoke testing. Connectors are registered at application startup and can be triggered via POST /api/connectors/{name}/sync.\n\n Chunking Strategy\n\nParsed text is split using a two-level hierarchical chunking approach implemented with LangChain's RecursiveCharacterTextSplitter. Parent chunks are 3200 characters with no overlap, while child chunks are 800 characters with 80 characters of overlap. Child chunks are what gets embedded and stored in ChromaDB. Each chunk carries metadata including a parent_id, the full parent_text, the doc_id, and the collection_id. During retrieval, child chunks are used for similarity search but the larger parent text is returned to the language model, providing richer context.\n\n Upsert Behaviour\n\nWhen a document is re-uploaded or a connector re-syncs a previously seen source, the system performs a content-aware upsert. Each document row stores a source_uri (the filename or connector source path) and a SHA-256 content_hash of the parsed text. On ingest, the system looks up any existing document with the same (collection_id, source_uri). If the hash matches, ingestion is skipped entirely. If the hash differs, new chunks are written first, then the old document row and its ChromaDB chunks are deleted \u2014 ensuring the old version is never lost due to a failed re-ingest.\n\n The Chat Pipeline\n\nWhen a user sends a message, the application runs a five-node LangGraph state machine.\n\nThe collection_router node checks whether the target ChromaDB collection exists and contains documents. If it is empty or missing, it returns an early exit message without invoking the language model.\n\nThe query_rewriter node reformulates the user's question to be standalone. On the first message of a conversation it passes the question through unchanged. On follow-up messages it uses the language model to rewrite the question using the last six messages of history, optimising it for document retrieval.\n\nThe retriever node embeds the rewritten query and performs a nearest-neighbour search in ChromaDB, fetching the top ten results. Results are deduplicated by parent_id and the unique parent texts are passed forward.\n\nThe reranker node applies a cross-encoder model (cross-encoder/ms-marco-MiniLM-L-6-v2 from sentence-transformers) to score each query-chunk pair. The top five chunks by score are selected.\n\nThe generator node constructs a system prompt that instructs the language model to answer only using the provided context chunks. It appends the conversation history and the current user message, then streams the response token by token back to the client via Server-Sent Events.\n\n",
      "<2-hop>\n\nStorage\n\nPostgreSQL stores collections, documents, conversations, messages, and connector sync state. ChromaDB stores the vector embeddings and chunk metadata. The two stores are linked by the collection UUID, which is used as the ChromaDB collection name. Deleting a collection removes both the PostgreSQL row and the corresponding ChromaDB collection.\n"
    ],
    "reference": "ChromaDB facilitates the storage and retrieval of document chunks in the Personal Knowledge Base by storing the vector embeddings and chunk metadata. Each chunk, which is created during the document ingestion process, carries metadata including a parent_id, the full parent_text, the doc_id, and the collection_id. During retrieval, these child chunks are used for similarity searches, while the larger parent text is returned to the language model for richer context. PostgreSQL, on the other hand, stores collections, documents, conversations, messages, and the state of connector syncs. The two storage systems are linked by the collection UUID, which serves as the ChromaDB collection name. When a collection is deleted, both the PostgreSQL row and the corresponding ChromaDB collection are removed, ensuring that the data remains synchronized.",
    "synthesizer_name": "multi_hop_specific_query_synthesizer"
  },
  {
    "user_input": "How does ChromaDB facilitate the storage of vector embeddings in the Personal Knowledge Base architecture?",
    "reference_contexts": [
      "<1-hop>\n\nPersonal Knowledge Base \u2014 Architecture Overview\n\nThe Personal Knowledge Base is a self-hosted application that lets users ingest documents and query them using a Retrieval-Augmented Generation (RAG) pipeline. It is built with FastAPI for the HTTP layer, PostgreSQL for relational metadata, ChromaDB for vector storage, and LangGraph for orchestrating the chat pipeline.\n\n Document Ingestion\n\nDocuments can be ingested in two ways: manual file upload and automated connector sync.\n\nFile uploads are accepted via a POST request to /api/collections/{collection_id}/documents. Supported formats are PDF, DOCX, and TXT. The raw file bytes are parsed into plain text using LangChain document loaders: PyPDFLoader for PDFs, Docx2txtLoader for Word documents, and TextLoader for plain text files.\n\nConnectors are registered components that implement a BaseConnector interface. Each connector exposes an async generator called sync() that yields LangChain Document objects. A DummyConnector is provided for smoke testing. Connectors are registered at application startup and can be triggered via POST /api/connectors/{name}/sync.\n\n Chunking Strategy\n\nParsed text is split using a two-level hierarchical chunking approach implemented with LangChain's RecursiveCharacterTextSplitter. Parent chunks are 3200 characters with no overlap, while child chunks are 800 characters with 80 characters of overlap. Child chunks are what gets embedded and stored in ChromaDB. Each chunk carries metadata including a parent_id, the full parent_text, the doc_id, and the collection_id. During retrieval, child chunks are used for similarity search but the larger parent text is returned to the language model, providing richer context.\n\n Upsert Behaviour\n\nWhen a document is re-uploaded or a connector re-syncs a previously seen source, the system performs a content-aware upsert. Each document row stores a source_uri (the filename or connector source path) and a SHA-256 content_hash of the parsed text. On ingest, the system looks up any existing document with the same (collection_id, source_uri). If the hash matches, ingestion is skipped entirely. If the hash differs, new chunks are written first, then the old document row and its ChromaDB chunks are deleted \u2014 ensuring the old version is never lost due to a failed re-ingest.\n\n The Chat Pipeline\n\nWhen a user sends a message, the application runs a five-node LangGraph state machine.\n\nThe collection_router node checks whether the target ChromaDB collection exists and contains documents. If it is empty or missing, it returns an early exit message without invoking the language model.\n\nThe query_rewriter node reformulates the user's question to be standalone. On the first message of a conversation it passes the question through unchanged. On follow-up messages it uses the language model to rewrite the question using the last six messages of history, optimising it for document retrieval.\n\nThe retriever node embeds the rewritten query and performs a nearest-neighbour search in ChromaDB, fetching the top ten results. Results are deduplicated by parent_id and the unique parent texts are passed forward.\n\nThe reranker node applies a cross-encoder model (cross-encoder/ms-marco-MiniLM-L-6-v2 from sentence-transformers) to score each query-chunk pair. The top five chunks by score are selected.\n\nThe generator node constructs a system prompt that instructs the language model to answer only using the provided context chunks. It appends the conversation history and the current user message, then streams the response token by token back to the client via Server-Sent Events.\n\n",
      "<2-hop>\n\nStorage\n\nPostgreSQL stores collections, documents, conversations, messages, and connector sync state. ChromaDB stores the vector embeddings and chunk metadata. The two stores are linked by the collection UUID, which is used as the ChromaDB collection name. Deleting a collection removes both the PostgreSQL row and the corresponding ChromaDB collection.\n"
    ],
    "reference": "ChromaDB facilitates the storage of vector embeddings in the Personal Knowledge Base architecture by storing the chunk metadata and vector embeddings generated during the document ingestion process. Each chunk, which is created from the parsed text using a hierarchical chunking strategy, is embedded and stored in ChromaDB. The connection between PostgreSQL and ChromaDB is established through the collection UUID, which serves as the ChromaDB collection name, ensuring that the vector embeddings are properly linked to their corresponding documents and collections.",
    "synthesizer_name": "multi_hop_specific_query_synthesizer"
  },
  {
    "user_input": "How does ChromaDB facilitate the storage and retrieval of document chunks in the Personal Knowledge Base architecture?",
    "reference_contexts": [
      "<1-hop>\n\nPersonal Knowledge Base \u2014 Architecture Overview\n\nThe Personal Knowledge Base is a self-hosted application that lets users ingest documents and query them using a Retrieval-Augmented Generation (RAG) pipeline. It is built with FastAPI for the HTTP layer, PostgreSQL for relational metadata, ChromaDB for vector storage, and LangGraph for orchestrating the chat pipeline.\n\n Document Ingestion\n\nDocuments can be ingested in two ways: manual file upload and automated connector sync.\n\nFile uploads are accepted via a POST request to /api/collections/{collection_id}/documents. Supported formats are PDF, DOCX, and TXT. The raw file bytes are parsed into plain text using LangChain document loaders: PyPDFLoader for PDFs, Docx2txtLoader for Word documents, and TextLoader for plain text files.\n\nConnectors are registered components that implement a BaseConnector interface. Each connector exposes an async generator called sync() that yields LangChain Document objects. A DummyConnector is provided for smoke testing. Connectors are registered at application startup and can be triggered via POST /api/connectors/{name}/sync.\n\n Chunking Strategy\n\nParsed text is split using a two-level hierarchical chunking approach implemented with LangChain's RecursiveCharacterTextSplitter. Parent chunks are 3200 characters with no overlap, while child chunks are 800 characters with 80 characters of overlap. Child chunks are what gets embedded and stored in ChromaDB. Each chunk carries metadata including a parent_id, the full parent_text, the doc_id, and the collection_id. During retrieval, child chunks are used for similarity search but the larger parent text is returned to the language model, providing richer context.\n\n Upsert Behaviour\n\nWhen a document is re-uploaded or a connector re-syncs a previously seen source, the system performs a content-aware upsert. Each document row stores a source_uri (the filename or connector source path) and a SHA-256 content_hash of the parsed text. On ingest, the system looks up any existing document with the same (collection_id, source_uri). If the hash matches, ingestion is skipped entirely. If the hash differs, new chunks are written first, then the old document row and its ChromaDB chunks are deleted \u2014 ensuring the old version is never lost due to a failed re-ingest.\n\n The Chat Pipeline\n\nWhen a user sends a message, the application runs a five-node LangGraph state machine.\n\nThe collection_router node checks whether the target ChromaDB collection exists and contains documents. If it is empty or missing, it returns an early exit message without invoking the language model.\n\nThe query_rewriter node reformulates the user's question to be standalone. On the first message of a conversation it passes the question through unchanged. On follow-up messages it uses the language model to rewrite the question using the last six messages of history, optimising it for document retrieval.\n\nThe retriever node embeds the rewritten query and performs a nearest-neighbour search in ChromaDB, fetching the top ten results. Results are deduplicated by parent_id and the unique parent texts are passed forward.\n\nThe reranker node applies a cross-encoder model (cross-encoder/ms-marco-MiniLM-L-6-v2 from sentence-transformers) to score each query-chunk pair. The top five chunks by score are selected.\n\nThe generator node constructs a system prompt that instructs the language model to answer only using the provided context chunks. It appends the conversation history and the current user message, then streams the response token by token back to the client via Server-Sent Events.\n\n",
      "<2-hop>\n\nStorage\n\nPostgreSQL stores collections, documents, conversations, messages, and connector sync state. ChromaDB stores the vector embeddings and chunk metadata. The two stores are linked by the collection UUID, which is used as the ChromaDB collection name. Deleting a collection removes both the PostgreSQL row and the corresponding ChromaDB collection.\n"
    ],
    "reference": "ChromaDB facilitates the storage and retrieval of document chunks in the Personal Knowledge Base architecture by storing the vector embeddings and chunk metadata. Each child chunk, which is embedded and stored in ChromaDB, carries metadata including a parent_id, the full parent_text, the doc_id, and the collection_id. During retrieval, these child chunks are used for similarity search, while the larger parent text is returned to the language model, providing richer context. Additionally, ChromaDB collections are linked to PostgreSQL by the collection UUID, ensuring that deleting a collection removes both the PostgreSQL row and the corresponding ChromaDB collection.",
    "synthesizer_name": "multi_hop_specific_query_synthesizer"
  },
  {
    "user_input": "How does ChromaDB interact with the Personal Knowledge Base for document storage and retrieval?",
    "reference_contexts": [
      "<1-hop>\n\nPersonal Knowledge Base \u2014 Architecture Overview\n\nThe Personal Knowledge Base is a self-hosted application that lets users ingest documents and query them using a Retrieval-Augmented Generation (RAG) pipeline. It is built with FastAPI for the HTTP layer, PostgreSQL for relational metadata, ChromaDB for vector storage, and LangGraph for orchestrating the chat pipeline.\n\n Document Ingestion\n\nDocuments can be ingested in two ways: manual file upload and automated connector sync.\n\nFile uploads are accepted via a POST request to /api/collections/{collection_id}/documents. Supported formats are PDF, DOCX, and TXT. The raw file bytes are parsed into plain text using LangChain document loaders: PyPDFLoader for PDFs, Docx2txtLoader for Word documents, and TextLoader for plain text files.\n\nConnectors are registered components that implement a BaseConnector interface. Each connector exposes an async generator called sync() that yields LangChain Document objects. A DummyConnector is provided for smoke testing. Connectors are registered at application startup and can be triggered via POST /api/connectors/{name}/sync.\n\n Chunking Strategy\n\nParsed text is split using a two-level hierarchical chunking approach implemented with LangChain's RecursiveCharacterTextSplitter. Parent chunks are 3200 characters with no overlap, while child chunks are 800 characters with 80 characters of overlap. Child chunks are what gets embedded and stored in ChromaDB. Each chunk carries metadata including a parent_id, the full parent_text, the doc_id, and the collection_id. During retrieval, child chunks are used for similarity search but the larger parent text is returned to the language model, providing richer context.\n\n Upsert Behaviour\n\nWhen a document is re-uploaded or a connector re-syncs a previously seen source, the system performs a content-aware upsert. Each document row stores a source_uri (the filename or connector source path) and a SHA-256 content_hash of the parsed text. On ingest, the system looks up any existing document with the same (collection_id, source_uri). If the hash matches, ingestion is skipped entirely. If the hash differs, new chunks are written first, then the old document row and its ChromaDB chunks are deleted \u2014 ensuring the old version is never lost due to a failed re-ingest.\n\n The Chat Pipeline\n\nWhen a user sends a message, the application runs a five-node LangGraph state machine.\n\nThe collection_router node checks whether the target ChromaDB collection exists and contains documents. If it is empty or missing, it returns an early exit message without invoking the language model.\n\nThe query_rewriter node reformulates the user's question to be standalone. On the first message of a conversation it passes the question through unchanged. On follow-up messages it uses the language model to rewrite the question using the last six messages of history, optimising it for document retrieval.\n\nThe retriever node embeds the rewritten query and performs a nearest-neighbour search in ChromaDB, fetching the top ten results. Results are deduplicated by parent_id and the unique parent texts are passed forward.\n\nThe reranker node applies a cross-encoder model (cross-encoder/ms-marco-MiniLM-L-6-v2 from sentence-transformers) to score each query-chunk pair. The top five chunks by score are selected.\n\nThe generator node constructs a system prompt that instructs the language model to answer only using the provided context chunks. It appends the conversation history and the current user message, then streams the response token by token back to the client via Server-Sent Events.\n\n",
      "<2-hop>\n\nStorage\n\nPostgreSQL stores collections, documents, conversations, messages, and connector sync state. ChromaDB stores the vector embeddings and chunk metadata. The two stores are linked by the collection UUID, which is used as the ChromaDB collection name. Deleting a collection removes both the PostgreSQL row and the corresponding ChromaDB collection.\n"
    ],
    "reference": "ChromaDB interacts with the Personal Knowledge Base by storing vector embeddings and chunk metadata. The two storage systems, PostgreSQL and ChromaDB, are linked by the collection UUID, which serves as the ChromaDB collection name. When a collection is deleted, both the PostgreSQL row and the corresponding ChromaDB collection are removed, ensuring that the data remains synchronized.",
    "synthesizer_name": "multi_hop_specific_query_synthesizer"
  }
]